<%= link_to "Create a new Kitten", new_kitten_path %>


<h1>Kittens : </h1>


<ul>
  <% @kittens.each do |kitten| %>
    <li> 
      <%= link_to kitten.name, kitten %> <%= link_to "Delete", kitten_path(kitten), data: { turbo_method: :delete, turbo_confirm: 'Do you want to delete this kitten?' } %>
    </li>
  <% end %>
</ul>