<%= link_to "Home", kittens_path %>

<%= render 'form' %>
<%= link_to "Delete #{@kitten.name}", kitten_path(@kitten), data: { turbo_method: :delete, turbo_confirm: 'Do you want to delete this kitten?' } %>
